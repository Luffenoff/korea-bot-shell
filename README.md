# Korea Bot — оболочка (ожидает исходник)

> Репозиторий `korea_bot` не найден локально и недоступен публично на GitHub (`Luffenoff/korea_bot`).
> Нужен актуальный GitHub-токен или путь к локальной копии для клонирования исходника.

## План оболочки

После получения исходника сюда попадёт:

- Код бота (без `.env`, токенов, БД)
- `config.example` вместо реальных конфигов
- README с инструкцией запуска

## Статус

`pending` — ждёт доступ к приватному репозиторию.
